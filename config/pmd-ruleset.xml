<?xml version="1.0"?>
<ruleset name="jmotif-quality" xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    Bug-focused PMD gate: error-prone + multithreading only. CloseResource is
    enforced; excluded rules below are pre-existing baseline findings (mostly
    stylistic or legacy patterns) so the gate blocks on new real defects.
  </description>

  <rule ref="category/java/errorprone.xml">
    <!-- pre-existing baseline: stylistic / legacy patterns -->
    <exclude name="AssignmentInOperand"/>
    <exclude name="AvoidDuplicateLiterals"/>
    <exclude name="AvoidFieldNameMatchingMethodName"/>
    <exclude name="AvoidFieldNameMatchingTypeName"/>
    <exclude name="AvoidLiteralsInIfCondition"/>
    <exclude name="CloneMethodMustBePublic"/>
    <exclude name="CloneMethodReturnTypeMustMatchClassName"/>
    <exclude name="CompareObjectsWithEquals"/>
    <exclude name="ConstructorCallsOverridableMethod"/>
    <exclude name="DoNotCallGarbageCollectionExplicitly"/>
    <exclude name="NullAssignment"/>
    <exclude name="ProperCloneImplementation"/>
    <exclude name="UnnecessaryCaseChange"/>
    <exclude name="UseLocaleWithCaseConversions"/>
  </rule>

  <rule ref="category/java/multithreading.xml">
    <!-- pre-existing baseline: stylistic / legacy patterns -->
    <exclude name="AvoidSynchronizedAtMethodLevel"/>
    <exclude name="AvoidSynchronizedStatement"/>
    <exclude name="AvoidUsingVolatile"/>
    <exclude name="DoNotUseThreads"/>
    <exclude name="UnsynchronizedStaticFormatter"/>
    <exclude name="UseConcurrentHashMap"/>
  </rule>

</ruleset>
